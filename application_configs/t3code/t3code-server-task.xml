<?xml version="1.0" encoding="UTF-8"?>
<!--
  Definition of the t3code-server scheduled task, the Windows stand-in for the
  Linux boot service. Deployed by src/deploy_configs.py (entry
  t3code_server_task) to %USERPROFILE%\.t3\t3code-server-task.xml and
  registered from there by scripts/setup_t3_server_windows.ps1, which fills in
  the two placeholders below - Task Scheduler keeps its own copy in the
  registry, so this file is the source of truth to diff against, not a live
  config. Re-run that script after changing anything here.

    __T3_TASK_USER__  -> DOMAIN\user of the account the serve runs as
    __T3_SERVE_CMD__  -> full path of the deployed ~\.t3\t3serve.cmd

  Settings that matter, all of them learned the hard way:
    ExecutionTimeLimit PT0S  the default when it is omitted is PT72H, which
                             closed out RyzenWhite's run on 2026-08-17 with
                             LastTaskResult 267014 (SCHED_S_TASK_TERMINATED)
                             and left the box with no server at all
    S4U principal            a default-principal task silently never starts
                             when nobody is logged on (LastTaskResult 267011)
    TimeTrigger + IgnoreNew  15-minute watchdog: a no-op while the task runs,
                             and the thing that restarts it within 15 minutes
                             when the task is not running for any reason
    StartWhenAvailable       run a missed boot/logon trigger late instead of
                             skipping it
    battery settings false   a serve must not be refused or stopped on
                             battery; harmless on a desktop, load-bearing on
                             the laptops that could run this later
-->
<Task version="1.3" xmlns="http://schemas.microsoft.com/windows/2004/02/mit/task">
  <RegistrationInfo>
    <URI>\t3code-server</URI>
    <Description>T3 Code headless server (runs ~\.t3\t3serve.cmd). Deployed from dotfiles.</Description>
  </RegistrationInfo>
  <Principals>
    <Principal id="Author">
      <UserId>__T3_TASK_USER__</UserId>
      <LogonType>S4U</LogonType>
      <RunLevel>LeastPrivilege</RunLevel>
    </Principal>
  </Principals>
  <Settings>
    <MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>
    <DisallowStartIfOnBatteries>false</DisallowStartIfOnBatteries>
    <StopIfGoingOnBatteries>false</StopIfGoingOnBatteries>
    <AllowHardTerminate>true</AllowHardTerminate>
    <StartWhenAvailable>true</StartWhenAvailable>
    <RunOnlyIfNetworkAvailable>false</RunOnlyIfNetworkAvailable>
    <IdleSettings>
      <StopOnIdleEnd>false</StopOnIdleEnd>
      <RestartOnIdle>false</RestartOnIdle>
    </IdleSettings>
    <AllowStartOnDemand>true</AllowStartOnDemand>
    <Enabled>true</Enabled>
    <Hidden>false</Hidden>
    <RunOnlyIfIdle>false</RunOnlyIfIdle>
    <DisallowStartOnRemoteAppSession>false</DisallowStartOnRemoteAppSession>
    <UseUnifiedSchedulingEngine>true</UseUnifiedSchedulingEngine>
    <WakeToRun>false</WakeToRun>
    <ExecutionTimeLimit>PT0S</ExecutionTimeLimit>
    <Priority>7</Priority>
    <RestartOnFailure>
      <Interval>PT1M</Interval>
      <Count>3</Count>
    </RestartOnFailure>
  </Settings>
  <Triggers>
    <BootTrigger>
      <Enabled>true</Enabled>
    </BootTrigger>
    <LogonTrigger>
      <Enabled>true</Enabled>
      <UserId>__T3_TASK_USER__</UserId>
    </LogonTrigger>
    <TimeTrigger>
      <StartBoundary>2026-01-01T00:00:00</StartBoundary>
      <Enabled>true</Enabled>
      <Repetition>
        <Interval>PT15M</Interval>
        <StopAtDurationEnd>false</StopAtDurationEnd>
      </Repetition>
    </TimeTrigger>
  </Triggers>
  <Actions Context="Author">
    <Exec>
      <Command>cmd.exe</Command>
      <Arguments>/c "__T3_SERVE_CMD__"</Arguments>
    </Exec>
  </Actions>
</Task>
