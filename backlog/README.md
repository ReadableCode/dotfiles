# Backlog

Real, understood problems in this repo that are not being fixed right now.
One file per issue, one row per issue here. Re-run an entry's `verify:` line
before acting on it — infrastructure moves on its own. Closing an entry means
deleting its file and its row in the same commit as the fix.

| Issue | Found | Status |
|-------|-------|--------|
| [flake8 is red at HEAD, so the documented lint gate cannot be used](flake8-red-at-head.md) | 2026-09-09 | open |
