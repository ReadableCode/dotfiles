# What any Windows box gets, whichever context owns it: the tools, the
# browser, the ssh server. What only some contexts' machines get lives in
# each context's <context>_app_lists.yaml, which src/app_lists.py adds to
# this list on member machines; a name two contexts both list is offered once.
7zip
angryip
autohotkey
firefox
golang
gsudo
logitech-options
powertoys
procexp
putty
speedtest
tailscale
wiztree
